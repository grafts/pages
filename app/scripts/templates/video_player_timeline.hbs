<div class="track">
	<div class="progress">
		<button class="script-add">
			<i class="icon-plus"></i>
		</button>
	</div>
</div>
<ul class="pointers">
	{{#if pointers}}
		{{#each pointers}}
			<li class="item" data-time="{{this.hhmmss}}" style="left: {{this.position}}%;">
				{{!-- <div class="time">{{this.hhmmss}}</div> --}}
				<div class="pointer" data-title="{{this.hhmmss}} / {{this.script}}" data-seq="{{this.seq}}"></div>
				<div class="summary">{{this.hhmmss}} / {{this.script}}</div>
			</li>
		{{/each}}
	{{/if}}
</ul>
