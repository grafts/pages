/*global define*/

define([
    'jquery',
    'backbone'
], function ($, Backbone) {
    'use strict';

    var VideoRouter = Backbone.Router.extend({
        routes: {
        }

    });

    return VideoRouter;
});
